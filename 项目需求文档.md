# 课题组资产管理系统 - 需求文档

## 1. 项目概述

### 1.1 项目名称
- **中文名**：课题组资产管理系统
- **英文名**：Lab Asset Management (LAM)

### 1.2 项目背景
课题组在日常科研工作中需要管理大量的仪器设备、试剂耗材等资产。传统的手工管理方式效率低下，容易出现资产丢失、库存不足、采购混乱等问题。本系统旨在通过信息化手段，实现资产的全生命周期管理，提高管理效率，降低管理成本。

### 1.3 项目目标
- 实现资产信息的数字化管理
- 规范资产采购、借用、领用流程
- 实时监控库存状态，避免物资短缺
- 提供数据统计分析，辅助决策
- 简化操作流程，提升用户体验

### 1.4 技术架构
- **后端框架**：Flask 3.0+
- **数据库**：SQLite 3+
- **前端技术**：React 18+ + Ant Design
- **认证系统**：JWT (JSON Web Token)
- **API设计**：RESTful API
- **部署方式**：Docker 容器化部署

---

## 2. 用户角色与权限

### 2.1 管理员 (Superuser)
**权限范围**：
- 系统所有功能的完全访问权限
- 审批采购申请
- 审批资产转移申请
- 审批领料申请
- 查看操作日志
- 添加/编辑/删除资产信息
- 添加/编辑/删除耗材信息
- 添加/编辑/删除供应商信息
- 添加/编辑/删除资产类别
- 查看所有统计数据
- 导出资产数据

### 2.2 普通用户 (Regular User)
**权限范围**：
- 查看资产列表和详情
- 查看耗材列表和库存
- 提交采购申请
- 提交资产转移申请
- 提交领料申请
- 借用可用资产
- 归还已借用的资产
- 查看自己的申请记录
- 查看自己的借用记录
- 查看自己的领料记录
- 查看统计数据（仅查看）

---

## 3. 功能需求

### 3.1 用户认证模块

#### 3.1.1 用户注册
- **功能描述**：新用户可以注册账号
- **输入字段**：
  - 用户名（必填，唯一）
  - 密码（必填，需确认）
  - 邮箱（可选）
- **业务规则**：
  - 用户名不能重复
  - 密码需符合安全要求
  - 注册成功后自动登录

#### 3.1.2 用户登录
- **功能描述**：已注册用户登录系统
- **输入字段**：
  - 用户名（必填）
  - 密码（必填）
- **业务规则**：
  - 验证用户名和密码
  - 登录成功后跳转到仪表盘

#### 3.1.3 用户登出
- **功能描述**：用户退出系统
- **业务规则**：
  - 清除会话信息
  - 跳转到首页

### 3.2 资产管理模块

#### 3.2.1 资产列表
- **功能描述**：查看所有资产信息
- **显示字段**：
  - 序号
  - 课题组资产编号
  - 学校资产编号
  - 资产名称
  - 型号
  - 规格
  - 生产厂家
  - 价格
  - 院系归属
  - 存放位置（存放房间/存放楼宇/存放校区）
  - 资产现状
  - 购买日期
  - 资产照片（资产全貌+资产型号铭牌近照+学校固定资产铭牌近照）
  - 课题组保管人姓名
  - 课题组保管人联系电话
- **资产分类**：按Tab标签页分类展示
  - 仪器设备
  - 软件
- **筛选功能**：
  - 按类别筛选
  - 按状态筛选
- **排序**：按创建时间倒序

#### 3.2.2 添加/编辑资产
- **功能描述**：管理员添加或编辑资产
- **输入字段**：
  - **基本信息**：
    - 资产名称（必填）
    - 课题组资产编号（必填，格式：DC+年份+序号）
    - 学校资产编号（可选）
    - 资产类型（必填，仪器设备/软件）
    - 型号（必填）
    - 规格（可选）
    - 生产厂家（可选）
    - 价格（可选）
    - 院系归属（必填）
    - 资产管理人（必填）
  - **存放信息**：
    - 存放校区（必填）
    - 存放楼宇（必填）
    - 存放房间（可选）
  - **保管人信息**：
    - 课题组保管人姓名（必填）
    - 课题组保管人联系电话（必填）
  - **资产照片**（全部必填）：
    - 资产全貌照片
    - 资产型号铭牌近照
    - 学校固定资产铭牌近照
  - **其他**：
    - 资产现状（在用/报废/报修/退库）
    - 购买日期（可选）
    - 资产情况备注（可选）
- **业务规则**：
  - 资产编号必须唯一
  - 默认资产现状为"在用"
  - 所有照片均为必填项

#### 3.2.3 资产现状枚举值
- `in_use` - 在用：资产正常使用中
- `scrapped` - 报废：资产已报废
- `repair` - 报修：资产正在维修中
- `returned` - 退库：资产已退回仓库

#### 3.2.4 资产管理人功能
- **功能描述**：每个资产指定一个资产管理人，支持交接变更
- **交接信息**：
  - 原管理人
  - 新管理人
  - 交接时间
  - 交接原因
  - 交接备注

### 3.3 耗材管理模块

#### 3.3.1 耗材列表
- **功能描述**：查看所有耗材信息
- **显示字段**：
  - 序号
  - 产品名称
  - 品牌
  - 货号（必填）
  - CAS号
  - 形态（固体/液体/气体）
  - 分类（耗材/试剂）
  - 是否危化品
  - 规格
  - 现存数量
  - 课题组保管人
  - 课题组保管人联系方式
  - 产品最早生产日期
  - 产品保质期
  - 是否过期
  - 存放位置（存放区域/存放房间/存放楼宇/存放校区）
  - 备注
- **筛选功能**：
  - 按类别筛选
  - 按低库存筛选（库存 < 最低库存）
  - 按是否过期筛选
- **预警提示**：
  - 库存低于最低库存时显示警告
  - 产品过期时显示警告

#### 3.3.2 添加/编辑耗材
- **功能描述**：管理员添加或编辑耗材
- **输入字段**：
  - 产品名称（必填）
  - 品牌（可选）
  - 货号（必填）
  - CAS号（可选，设备无CAS号）
  - 形态（固体/液体/气体）
  - 分类（耗材/试剂）
  - 是否危化品（是/否）
  - 规格（可选）
  - 现存数量（必填）
  - 课题组保管人（必填，默认为首次申请人）
  - 课题组保管人联系方式（必填）
  - 产品最早生产日期（可选）
  - 产品保质期（可选）
  - 存放区域（可选）
  - 存放房间（可选）
  - 存放楼宇（可选）
  - 存放校区（可选）
  - 备注（可选）
- **业务规则**：
  - 耗材编号自动生成格式：CON-XXXX
  - 耗材同一名称&品牌*货号*规格的设为一类计算总库存
  - 试剂同一名称&品牌*货号的设为一类计算总库存
  - 库存低于最低库存时需要预警
  - 过期产品需要标识

### 3.4 采购管理模块

#### 3.4.1 采购申请
- **功能描述**：用户提交采购申请
- **输入字段**：
  - **基本信息**：
    - 序号（自动生成）
    - 产品名称（必填）
    - 品牌（可选）
    - 货号（必填）
    - CAS号（可选，设备无CAS号）
    - 形态（固体/液体/气体/设备）
    - 规格（可选）
    - 数量（必填，默认1）
    - 用途（必填）
  - **申请人信息**：
    - 申请人（自动获取当前用户）
    - 申请项目（必填）
    - 申请日期（自动获取当前日期）
    - 申请人收货信息（必填，具体地址+姓名+电话）
  - **采购信息**：
    - 查询单价（元）
    - 订购单价（元）
    - 订购数量
    - 到货日期
    - 到货数量
  - **存放信息**：
    - 存放房间
    - 存放楼宇
    - 存放校区
  - **采购渠道**：
    - 是否危化品平台采购（是/否）
    - 是否生科院试剂中心采购（是/否）
    - 是否学校仓库采购（是/否）
  - **其他**：
    - 质量问题正式的书面说明及拍照证据
    - 备注
- **业务规则**：
  - 申请提交后状态为"待审批"
  - 记录申请人和申请时间
  - 记录操作日志

#### 3.4.2 采购审批
- **功能描述**：管理员审批采购申请
- **审批操作**：
  - 批准：状态改为"已审批"，记录审批人和审批时间
  - 拒绝：状态改为"已拒绝"，记录审批人
- **审批列表**：
  - 待审批列表
  - 已批准列表（最近10条）
  - 已拒绝列表（最近10条）

#### 3.4.3 我的采购申请
- **功能描述**：用户查看自己的采购申请记录
- **显示信息**：
  - 序号
  - 产品名称
  - 品牌
  - 货号
  - 数量
  - 订购单价
  - 状态
  - 申请日期
  - 审批人（如已审批）

### 3.5 资产业务模块

#### 3.5.1 模块结构
资产管理模块按以下子模块组织：
```
资产管理
├── 我保管的资产        // 个人保管的资产列表
├── 变动业务            // 保管人变更、资产调拨
├── 维修业务            // 设备维修申请与审批
└── 资产处置            // 退库、报废、报失报损
```

#### 3.5.2 我保管的资产 - 申请办理业务
- **功能入口**：在我保管的资产列表页面，提供"申请办理业务"按钮
- **业务类型（下拉选择）**：
  1. 申请保管人变更
  2. 申请资产调拨
  3. 申请设备维修
  4. 申请退库
  5. 申请报废
  6. 申请报失报损

#### 3.5.3 变动业务模块
**负责业务**：保管人变更、资产调拨

**保管人变更流程**：
```
申请人 → 审批人 → 资产管理员 → 完成
(发起)   (审核)   (确认)   (更新)
```

**资产调拨流程**：
```
申请人 → 审批人 → 接收确认 → 完成
(发起)   (审核)   (确认)   (更新)
```

**保管人变更申请字段**：
- 资产编号
- 原保管人
- 新保管人
- 变更原因
- 申请人
- 申请时间
- 状态
- 审批意见
- 审批人
- 审批时间

**资产调拨申请字段**：
- 资产编号
- 原存放位置
- 新存放位置（校区/楼宇/房间）
- 调拨原因
- 申请人
- 申请时间
- 状态
- 接收确认人
- 审批意见
- 审批人
- 审批时间

#### 3.5.4 维修业务模块
**负责业务**：设备维修

**设备维修流程**：
```
申请人 → 审批人 → 维修处理 → 完成
(发起)   (审核)   (维修)   (更新状态)
```

**维修业务字段**：
- 资产编号
- 故障描述
- 维修申请人
- 申请时间
- 维修状态（待审批/维修中/已完成）
- 维修记录
- 审批意见
- 审批人
- 审批时间

#### 3.5.5 资产处置模块
**负责业务**：退库、报废、报失报损

**退库流程**：
```
申请人 → 审批人 → 仓库确认 → 完成
(发起)   (审核)   (接收)   (更新)
```

**报废流程**：
```
申请人 → 审批人 → 资产管理员 → 完成
(发起)   (审核)   (确认)   (更新状态)
```

**报失报损流程**：
```
申请人 → 审批人 → 资产管理员 → 完成
(发起)   (审核)   (确认)   (更新状态)
```

**资产处置申请字段**：
- 资产编号
- 处置类型（退库/报废/报失报损）
- 处置原因
- 申请人
- 申请时间
- 状态
- 审批意见
- 审批人
- 审批时间

### 3.6 资产借用模块

#### 3.6.1 借用申请
- **功能描述**：用户借用可用资产
- **输入字段**：
  - 选择资产（仅显示可用状态的资产）
  - 用途（可选）
- **业务规则**：
  - 只能借用状态为"在用"的资产
  - 借用成功后资产现状改为"外借"
  - 创建借用记录
  - 记录操作日志

#### 3.6.2 归还资产
- **功能描述**：用户归还已借用的资产
- **业务规则**：
  - 只能归还自己借用的资产
  - 归还成功后资产现状改为"在用"
  - 更新借用记录的归还日期和状态
  - 记录操作日志

#### 3.6.3 我的借用记录
- **功能描述**：用户查看自己的借用记录
- **显示信息**：
  - 资产名称
  - 借用日期
  - 归还日期（如已归还）
  - 用途
  - 状态（借用中/已归还）

### 3.7 领料管理模块

#### 3.7.1 领料申请
- **功能描述**：用户申请领用耗材
- **输入字段**：
  - 选择耗材（仅显示库存>0的耗材）
  - 领用数量（必填）
  - 用途（可选）
- **业务规则**：
  - 申请提交后状态为"待审批"
  - 不立即扣减库存，等待审批
  - 记录申请人和申请时间

#### 3.7.2 领料审批
- **功能描述**：管理员审批领料申请
- **审批操作**：
  - 批准：
    - 检查库存是否充足
    - 扣减库存
    - 状态改为"已批准"
    - 记录审批人和审批时间
  - 拒绝：
    - 状态改为"已拒绝"
    - 记录审批人
- **业务规则**：
  - 库存不足时不能批准
  - 显示当前库存信息

#### 3.7.3 我的领料记录
- **功能描述**：用户查看自己的领料记录
- **显示信息**：
  - 物品名称
  - 领用数量
  - 用途
  - 状态（待审批/已批准/已拒绝）
  - 申请时间
  - 审批人（如已审批）

### 3.8 资产转移模块

#### 3.8.1 资产转移申请
- **功能描述**：用户申请转移资产位置
- **输入字段**：
  - 选择资产
  - 新位置（必填，包括校区/楼宇/房间）
  - 转移原因（必填）
- **业务规则**：
  - 申请提交后状态为"待审批"
  - 记录原位置和新位置
  - 记录申请人和申请时间

#### 3.8.2 资产转移审批
- **功能描述**：管理员审批资产转移申请
- **审批操作**：
  - 批准：
    - 更新资产位置为新位置
    - 状态改为"已批准"
    - 记录审批人和审批时间
  - 拒绝：
    - 状态改为"已拒绝"
    - 记录审批人
- **审批列表**：
  - 待审批列表
  - 已批准列表（最近10条）
  - 已拒绝列表（最近10条）

#### 3.8.3 我的转移申请
- **功能描述**：用户查看自己的转移申请记录
- **显示信息**：
  - 资产名称
  - 原位置
  - 新位置
  - 转移原因
  - 状态
  - 申请时间
  - 审批人（如已审批）

### 3.9 统计分析模块

#### 3.9.1 仪表盘
- **功能描述**：用户登录后的首页，显示关键信息
- **显示内容**：
  - 资产总数
  - 耗材总数
  - 我的借用中数量
  - 我的采购申请数量
  - 低库存耗材列表
  - 过期耗材列表
  - 管理员专属：
    - 待审批采购数量
    - 待审批转移数量
    - 待审批领料数量

#### 3.9.2 数据统计
- **功能描述**：查看各类统计数据
- **统计内容**：
  - 资产按现状统计
  - 资产按类别统计（仪器设备/软件）
  - 资产按院系统计
  - 资产按校区统计
  - 耗材总价值
  - 低库存耗材数量
  - 过期耗材数量
  - 采购申请按状态统计
  - 采购总预算
  - 当前借用中数量

#### 3.9.3 数据导出
- **功能描述**：导出资产数据为CSV文件
- **导出字段**：
  - 序号
  - 课题组资产编号
  - 学校资产编号
  - 名称
  - 型号
  - 规格
  - 生产厂家
  - 价格
  - 院系归属
  - 存放位置
  - 资产现状
  - 课题组保管人姓名
  - 课题组保管人联系电话

### 3.10 操作日志模块

#### 3.10.1 操作日志查看
- **功能描述**：管理员查看系统操作日志
- **权限**：仅管理员可访问
- **显示内容**：
  - 操作人
  - 操作类型
  - 操作对象
  - 操作时间
  - 操作详情
- **显示限制**：最近100条记录

### 3.11 基础数据管理模块

#### 3.11.1 资产类别管理
- **功能描述**：管理资产类别
- **类别层级**：支持多级分类
- **主要类别**：
  - 试剂
  - 耗材
  - 仪器
  - 软件
  - 其他
- **管理操作**：
  - 添加类别
  - 编辑类别
  - 删除类别（需检查是否被使用）

#### 3.11.2 供应商管理
- **功能描述**：管理供应商信息
- **信息字段**：
  - 供应商名称（必填）
  - 联系人（可选）
  - 电话（可选）
  - 邮箱（可选）
  - 地址（可选）
- **管理操作**：
  - 添加供应商
  - 编辑供应商
  - 删除供应商（需检查是否被使用）

### 3.12 帮助文档模块

#### 3.12.1 使用帮助
- **功能描述**：提供系统使用说明
- **内容**：
  - 系统简介
  - 功能模块说明
  - 操作指南
  - 常见问题解答

---

## 4. 数据模型

### 4.1 资产类别 (Category)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| name | String(100) | 类别名称 | 是 |
| parent | ForeignKey | 父类别 | 否 |
| created_at | DateTime | 创建时间 | 是 |

### 4.2 供应商 (Supplier)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| name | String(200) | 供应商名称 | 是 |
| contact | String(100) | 联系人 | 否 |
| phone | String(20) | 电话 | 否 |
| email | String | 邮箱 | 否 |
| address | Text | 地址 | 否 |
| created_at | DateTime | 创建时间 | 是 |

### 4.3 资产 (Asset)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| lab_asset_code | String(50) | 课题组资产编号（格式：DC+年份+序号） | 是 |
| school_asset_code | String(50) | 学校资产编号 | 否 |
| name | String(200) | 资产名称 | 是 |
| asset_type | String(20) | 资产类型（equipment-仪器设备，software-软件） | 是 |
| model | String(100) | 型号 | 是 |
| specifications | Text | 规格 | 否 |
| manufacturer | String(200) | 生产厂家 | 否 |
| purchase_price | Decimal(12,2) | 采购价格 | 否 |
| department | String(100) | 院系归属 | 是 |
| current_status | String(20) | 资产现状 | 是 |
| campus | String(50) | 存放校区 | 是 |
| building | String(100) | 存放楼宇 | 是 |
| room | String(50) | 存放房间 | 否 |
| purchase_date | Date | 采购日期 | 否 |
| custodian_name | String(50) | 课题组保管人姓名 | 是 |
| custodian_phone | String(20) | 课题组保管人联系电话 | 是 |
| manager_id | Integer | 资产管理人ID | 是 |
| photo_full_url | String(500) | 资产全貌照片URL | 是 |
| photo_model_url | String(500) | 资产型号铭牌近照URL | 是 |
| photo_tag_url | String(500) | 学校固定资产铭牌近照URL | 是 |
| remarks | Text | 备注 | 否 |
| created_at | DateTime | 创建时间 | 是 |
| updated_at | DateTime | 更新时间 | 是 |

**资产现状枚举值**：
- `in_use` - 在用
- `scrapped` - 报废
- `repair` - 报修
- `returned` - 退库
- `borrowed` - 外借

### 4.4 耗材 (Consumable)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| name | String(200) | 耗材名称 | 是 |
| code | String(50) | 耗材编号 | 是 |
| category | ForeignKey | 类别 | 是 |
| supplier | ForeignKey | 供应商 | 否 |
| brand | String(100) | 品牌 | 否 |
| product_code | String(100) | 货号 | 是 |
| cas_number | String(50) | CAS号 | 否 |
| form | String(20) | 形态（固体/液体/气体） | 否 |
| consumable_type | String(20) | 分类（耗材/试剂） | 是 |
| is_hazardous | Boolean | 是否危化品 | 是 |
| specifications | String(200) | 规格 | 否 |
| stock | Integer | 库存数量 | 是 |
| min_stock | Integer | 最低库存 | 是 |
| custodian_name | String(50) | 课题组保管人 | 是 |
| custodian_phone | String(20) | 课题组保管人联系方式 | 是 |
| production_date | Date | 产品最早生产日期 | 否 |
| expiration_date | Date | 产品保质期 | 否 |
| is_expired | Boolean | 是否过期 | 否 |
| storage_area | String(100) | 存放区域 | 否 |
| room | String(50) | 存放房间 | 否 |
| building | String(100) | 存放楼宇 | 否 |
| campus | String(50) | 存放校区 | 否 |
| price | Decimal(10,2) | 单价 | 否 |
| remarks | Text | 备注 | 否 |
| created_at | DateTime | 创建时间 | 是 |
| updated_at | DateTime | 更新时间 | 是 |

### 4.5 采购申请 (PurchaseRequest)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| sequence_number | Integer | 序号 | 是 |
| product_name | String(200) | 产品名称 | 是 |
| brand | String(100) | 品牌 | 否 |
| product_code | String(100) | 货号 | 是 |
| cas_number | String(50) | CAS号 | 否 |
| form | String(20) | 形态（固体/液体/气体/设备） | 否 |
| specifications | String(200) | 规格 | 否 |
| quantity | Integer | 数量 | 是 |
| purpose | Text | 用途 | 是 |
| applicant | ForeignKey | 申请人 | 是 |
| project_name | String(200) | 申请项目 | 是 |
| application_date | Date | 申请日期 | 是 |
| delivery_info | Text | 申请人收货信息 | 是 |
| query_price | Decimal(12,2) | 查询单价 | 否 |
| order_price | Decimal(12,2) | 订购单价 | 否 |
| order_quantity | Integer | 订购数量 | 否 |
| delivery_date | Date | 到货日期 | 否 |
| delivery_quantity | Integer | 到货数量 | 否 |
| campus | String(50) | 存放校区 | 否 |
| building | String(100) | 存放楼宇 | 否 |
| room | String(50) | 存放房间 | 否 |
| is_hazardous_platform | Boolean | 是否危化品平台采购 | 否 |
| is_institute_center | Boolean | 是否生科院试剂中心采购 | 否 |
| is_school_warehouse | Boolean | 是否学校仓库采购 | 否 |
| quality_issue_info | Text | 质量问题说明及证据 | 否 |
| remarks | Text | 备注 | 否 |
| status | String(20) | 状态 | 是 |
| approver | ForeignKey | 审批人 | 否 |
| approval_comment | Text | 审批意见 | 否 |
| approved_at | DateTime | 审批时间 | 否 |
| created_at | DateTime | 创建时间 | 是 |

**状态枚举值**：
- `pending` - 待审批
- `approved` - 已审批
- `purchased` - 已采购
- `rejected` - 已拒绝

### 4.6 资产管理人交接记录 (AssetManagerTransfer)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| asset | ForeignKey | 资产 | 是 |
| old_manager | ForeignKey | 原管理人 | 是 |
| new_manager | ForeignKey | 新管理人 | 是 |
| transfer_time | DateTime | 交接时间 | 是 |
| transfer_reason | Text | 交接原因 | 否 |
| remarks | Text | 备注 | 否 |
| created_by | ForeignKey | 创建人 | 是 |
| created_at | DateTime | 创建时间 | 是 |

### 4.7 资产业务申请 (AssetApplication)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| asset | ForeignKey | 资产 | 是 |
| application_type | String(30) | 申请类型 | 是 |
| applicant | ForeignKey | 申请人 | 是 |
| applicant_name | String(50) | 申请人姓名 | 是 |
| application_time | DateTime | 申请时间 | 是 |
| status | String(20) | 状态 | 是 |
| content | Text | 申请内容（JSON格式） | 是 |
| approval_comment | Text | 审批意见 | 否 |
| approver | ForeignKey | 审批人 | 否 |
| approval_time | DateTime | 审批时间 | 否 |
| created_at | DateTime | 创建时间 | 是 |
| updated_at | DateTime | 更新时间 | 是 |

**申请类型枚举值**：
- `custodian_change` - 保管人变更
- `allocation` - 资产调拨
- `repair` - 设备维修
- `return` - 退库
- `scrap` - 报废
- `loss` - 报失报损

**状态枚举值**：
- `pending` - 待审批
- `approved` - 已审批
- `rejected` - 已拒绝
- `processing` - 处理中
- `completed` - 已完成

### 4.8 资产转移 (AssetTransfer)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| asset | ForeignKey | 资产 | 是 |
| from_campus | String(50) | 原校区 | 是 |
| from_building | String(100) | 原楼宇 | 是 |
| from_room | String(50) | 原房间 | 是 |
| to_campus | String(50) | 新校区 | 是 |
| to_building | String(100) | 新楼宇 | 是 |
| to_room | String(50) | 新房间 | 是 |
| reason | Text | 转移原因 | 是 |
| applicant | ForeignKey | 申请人 | 是 |
| status | String(20) | 状态 | 是 |
| receiver | ForeignKey | 接收确认人 | 否 |
| approver | ForeignKey | 审批人 | 否 |
| approved_at | DateTime | 审批时间 | 否 |
| created_at | DateTime | 创建时间 | 是 |

**状态枚举值**：
- `pending` - 待审批
- `approved` - 已批准
- `rejected` - 已拒绝

### 4.9 借还记录 (BorrowRecord)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| asset | ForeignKey | 资产 | 是 |
| borrower | ForeignKey | 借用人 | 是 |
| borrow_date | DateTime | 借用日期 | 是 |
| return_date | DateTime | 归还日期 | 否 |
| purpose | Text | 用途 | 否 |
| status | String(20) | 状态 | 是 |

**状态枚举值**：
- `borrowed` - 借用中
- `returned` - 已归还

### 4.10 领料记录 (PickRecord)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| item | ForeignKey | 物品（耗材） | 是 |
| picker | ForeignKey | 领用人 | 是 |
| quantity | Integer | 领取数量 | 是 |
| purpose | Text | 用途 | 否 |
| status | String(20) | 状态 | 是 |
| approver | ForeignKey | 审批人 | 否 |
| approved_at | DateTime | 审批时间 | 否 |
| created_at | DateTime | 创建时间 | 是 |

**状态枚举值**：
- `pending` - 待审批
- `approved` - 已批准
- `rejected` - 已拒绝

### 4.11 操作日志 (OperationLog)
| 字段名 | 类型 | 说明 | 必填 |
|--------|------|------|------|
| id | Integer | 主键 | 是 |
| user | ForeignKey | 操作人 | 是 |
| action | String(50) | 操作类型 | 是 |
| model | String(50) | 操作对象模型 | 是 |
| object_id | Integer | 对象ID | 是 |
| object_repr | String(200) | 对象表示 | 是 |
| details | Text | 操作详情 | 否 |
| timestamp | DateTime | 操作时间 | 是 |

---

## 5. 业务流程

### 5.1 资产业务整体流程
```
我保管的资产
      │
      ▼
  申请办理业务
      │
┌─────┼─────┐
│     │     │
▼     ▼     ▼
变动业务 维修业务 资产处置
│         │     │
▼         ▼     ▼
保管人变更  设备维修  退库
资产调拨           报废
                  报失报损
```

### 5.2 资产借用流程
```
用户登录 → 查看可用资产 → 选择资产并填写用途 → 提交借用申请
→ 系统自动批准 → 资产状态改为"外借" → 创建借用记录
→ 使用完毕 → 用户归还资产 → 资产状态改为"在用" → 更新借用记录
```

### 5.3 采购申请流程
```
用户登录 → 填写采购申请信息 → 提交申请 → 状态为"待审批"
→ 管理员审批 →
  ├─ 批准：状态改为"已审批"，记录审批人和时间
  └─ 拒绝：状态改为"已拒绝"，记录审批人
→ 用户查看审批结果
```

### 5.4 领料申请流程
```
用户登录 → 选择耗材并填写数量和用途 → 提交申请 → 状态为"待审批"
→ 管理员审批 →
  ├─ 批准：检查库存 → 扣减库存 → 状态改为"已批准"
  └─ 拒绝：状态改为"已拒绝"
→ 用户查看审批结果
```

### 5.5 资产转移流程
```
用户登录 → 选择资产并填写新位置和原因 → 提交申请 → 状态为"待审批"
→ 管理员审批 →
  ├─ 批准：更新资产位置 → 状态改为"已批准"
  └─ 拒绝：状态改为"已拒绝"
→ 用户查看审批结果
```

### 5.6 保管人变更流程
```
用户登录 → 申请办理业务 → 申请保管人变更
→ 填写新保管人和变更原因 → 提交申请 → 状态为"待审批"
→ 审批人审核 → 资产管理员确认 → 更新资产保管人 → 完成
```

### 5.7 设备维修流程
```
用户登录 → 申请办理业务 → 申请设备维修
→ 填写故障描述 → 提交申请 → 状态为"待审批"
→ 审批人审核 → 维修处理 → 状态改为"已完成"
```

### 5.8 资产处置流程
```
用户登录 → 申请办理业务 → 选择处置类型
├─ 退库：填写原因 → 审批 → 仓库确认 → 完成
├─ 报废：填写原因 → 审批 → 资产管理员确认 → 状态改为"报废"
└─ 报失报损：填写原因 → 审批 → 资产管理员确认 → 状态更新
```

---

## 6. API接口设计

### 6.1 认证相关接口

#### 6.1.1 用户注册
- **接口路径**：`POST /api/auth/register`
- **请求参数**：
  ```json
  {
    "username": "string",
    "password": "string",
    "email": "string (optional)"
  }
  ```
- **响应**：
  ```json
  {
    "success": true,
    "message": "注册成功",
    "data": {
      "user_id": 1,
      "username": "string",
      "token": "jwt_token_string"
    }
  }
  ```

#### 6.1.2 用户登录
- **接口路径**：`POST /api/auth/login`
- **请求参数**：
  ```json
  {
    "username": "string",
    "password": "string"
  }
  ```
- **响应**：
  ```json
  {
    "success": true,
    "message": "登录成功",
    "data": {
      "user_id": 1,
      "username": "string",
      "is_superuser": false,
      "token": "jwt_token_string"
    }
  }
  ```

#### 6.1.3 获取当前用户信息
- **接口路径**：`GET /api/auth/me`
- **请求头**：`Authorization: Bearer <token>`
- **响应**：
  ```json
  {
    "success": true,
    "data": {
      "id": 1,
      "username": "string",
      "email": "string",
      "is_superuser": false
    }
  }
  ```

### 6.2 资产管理接口

#### 6.2.1 获取资产列表
- **接口路径**：`GET /api/assets`
- **请求头**：`Authorization: Bearer <token>`
- **查询参数**：
  - `asset_type` (可选)：按资产类型筛选（equipment/software）
  - `status` (可选)：按状态筛选
  - `category_id` (可选)：按类别筛选
  - `campus` (可选)：按校区筛选
  - `manager_id` (可选)：按资产管理人筛选
  - `page` (可选)：页码，默认1
  - `page_size` (可选)：每页数量，默认20
- **响应**：
  ```json
  {
    "success": true,
    "data": {
      "items": [
        {
          "id": 1,
          "lab_asset_code": "DC2026001",
          "school_asset_code": "string",
          "name": "string",
          "asset_type": "equipment",
          "model": "string",
          "specifications": "string",
          "manufacturer": "string",
          "purchase_price": 1000.00,
          "department": "string",
          "current_status": "in_use",
          "campus": "string",
          "building": "string",
          "room": "string",
          "custodian_name": "string",
          "custodian_phone": "string",
          "manager": {"id": 1, "username": "string"}
        }
      ],
      "total": 100,
      "page": 1,
      "page_size": 20
    }
  }
  ```

#### 6.2.2 获取资产详情
- **接口路径**：`GET /api/assets/<asset_id>`
- **请求头**：`Authorization: Bearer <token>`
- **响应**：
  ```json
  {
    "success": true,
    "data": {
      "id": 1,
      "lab_asset_code": "DC2026001",
      "school_asset_code": "string",
      "name": "string",
      "asset_type": "equipment",
      "model": "string",
      "specifications": "string",
      "manufacturer": "string",
      "purchase_price": 1000.00,
      "department": "string",
      "current_status": "in_use",
      "campus": "string",
      "building": "string",
      "room": "string",
      "purchase_date": "2024-01-01",
      "custodian_name": "string",
      "custodian_phone": "string",
      "manager": {"id": 1, "username": "string"},
      "photo_full_url": "string",
      "photo_model_url": "string",
      "photo_tag_url": "string",
      "remarks": "string",
      "created_at": "2024-01-01T00:00:00Z",
      "updated_at": "2024-01-01T00:00:00Z"
    }
  }
  ```

#### 6.2.3 创建资产
- **接口路径**：`POST /api/assets`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **请求参数**：
  ```json
  {
    "lab_asset_code": "DC2026001",
    "school_asset_code": "string",
    "name": "string",
    "asset_type": "equipment",
    "model": "string",
    "specifications": "string",
    "manufacturer": "string",
    "purchase_price": 1000.00,
    "department": "string",
    "campus": "string",
    "building": "string",
    "room": "string",
    "custodian_name": "string",
    "custodian_phone": "string",
    "manager_id": 1,
    "purchase_date": "2024-01-01",
    "photo_full_url": "string",
    "photo_model_url": "string",
    "photo_tag_url": "string",
    "remarks": "string"
  }
  ```
- **响应**：
  ```json
  {
    "success": true,
    "message": "资产创建成功",
    "data": {
      "id": 1,
      "name": "string",
      "lab_asset_code": "DC2026001"
    }
  }
  ```

#### 6.2.4 更新资产
- **接口路径**：`PUT /api/assets/<asset_id>`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **请求参数**：同创建资产
- **响应**：
  ```json
  {
    "success": true,
    "message": "资产更新成功",
    "data": {
      "id": 1,
      "name": "string"
    }
  }
  ```

#### 6.2.5 删除资产
- **接口路径**：`DELETE /api/assets/<asset_id>`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **响应**：
  ```json
  {
    "success": true,
    "message": "资产删除成功"
  }
  ```

#### 6.2.6 资产管理人交接
- **接口路径**：`POST /api/assets/<asset_id>/transfer-manager`
- **请求头**：`Authorization: Bearer <token>`
- **请求参数**：
  ```json
  {
    "new_manager_id": 2,
    "transfer_reason": "string",
    "remarks": "string"
  }
  ```

### 6.3 耗材管理接口

#### 6.3.1 获取耗材列表
- **接口路径**：`GET /api/consumables`
- **请求头**：`Authorization: Bearer <token>`
- **查询参数**：
  - `category_id` (可选)：按类别筛选
  - `consumable_type` (可选)：按分类筛选（耗材/试剂）
  - `low_stock` (可选)：是否只显示低库存
  - `is_expired` (可选)：是否只显示过期
  - `page` (可选)：页码
  - `page_size` (可选)：每页数量
- **响应**：
  ```json
  {
    "success": true,
    "data": {
      "items": [
        {
          "id": 1,
          "name": "string",
          "code": "string",
          "brand": "string",
          "product_code": "string",
          "cas_number": "string",
          "form": "固体",
          "consumable_type": "耗材",
          "is_hazardous": false,
          "specifications": "string",
          "stock": 100,
          "min_stock": 10,
          "custodian_name": "string",
          "custodian_phone": "string",
          "production_date": "2024-01-01",
          "expiration_date": "2025-01-01",
          "is_expired": false,
          "storage_area": "string",
          "room": "string",
          "building": "string",
          "campus": "string",
          "price": 10.00,
          "is_low_stock": false
        }
      ],
      "total": 50,
      "page": 1,
      "page_size": 20
    }
  }
  ```

#### 6.3.2 创建耗材
- **接口路径**：`POST /api/consumables`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **请求参数**：
  ```json
  {
    "name": "string",
    "category_id": 1,
    "supplier_id": 1,
    "brand": "string",
    "product_code": "string",
    "cas_number": "string",
    "form": "固体",
    "consumable_type": "耗材",
    "is_hazardous": false,
    "specifications": "string",
    "stock": 100,
    "min_stock": 10,
    "custodian_name": "string",
    "custodian_phone": "string",
    "production_date": "2024-01-01",
    "expiration_date": "2025-01-01",
    "storage_area": "string",
    "room": "string",
    "building": "string",
    "campus": "string",
    "price": 10.00,
    "remarks": "string"
  }
  ```

### 6.4 采购管理接口

#### 6.4.1 创建采购申请
- **接口路径**：`POST /api/purchases`
- **请求头**：`Authorization: Bearer <token>`
- **请求参数**：
  ```json
  {
    "product_name": "string",
    "brand": "string",
    "product_code": "string",
    "cas_number": "string",
    "form": "固体",
    "specifications": "string",
    "quantity": 10,
    "purpose": "string",
    "project_name": "string",
    "delivery_info": "string",
    "campus": "string",
    "building": "string",
    "room": "string",
    "is_hazardous_platform": false,
    "is_institute_center": false,
    "is_school_warehouse": false,
    "remarks": "string"
  }
  ```
- **响应**：
  ```json
  {
    "success": true,
    "message": "采购申请已提交",
    "data": {
      "id": 1,
      "status": "pending"
    }
  }
  ```

#### 6.4.2 获取采购申请列表
- **接口路径**：`GET /api/purchases`
- **请求头**：`Authorization: Bearer <token>`
- **查询参数**：
  - `status` (可选)：按状态筛选
  - `applicant_id` (可选)：按申请人筛选
  - `page` (可选)：页码
  - `page_size` (可选)：每页数量

#### 6.4.3 审批采购申请
- **接口路径**：`POST /api/purchases/<request_id>/approve`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **请求参数**：
  ```json
  {
    "action": "approve",
    "approval_comment": "string"
  }
  ```

### 6.5 资产业务接口

#### 6.5.1 创建业务申请
- **接口路径**：`POST /api/asset-applications`
- **请求头**：`Authorization: Bearer <token>`
- **请求参数**：
  ```json
  {
    "asset_id": 1,
    "application_type": "custodian_change",
    "content": {
      "new_custodian_id": 2,
      "reason": "string"
    }
  }
  ```

#### 6.5.2 获取业务申请列表
- **接口路径**：`GET /api/asset-applications`
- **请求头**：`Authorization: Bearer <token>`
- **查询参数**：
  - `application_type` (可选)：申请类型
  - `status` (可选)：状态
  - `applicant_id` (可选)：申请人

#### 6.5.3 审批业务申请
- **接口路径**：`POST /api/asset-applications/<application_id>/approve`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员/资产管理员
- **请求参数**：
  ```json
  {
    "action": "approve",
    "approval_comment": "string"
  }
  ```

### 6.6 借用管理接口

#### 6.6.1 借用资产
- **接口路径**：`POST /api/borrows`
- **请求头**：`Authorization: Bearer <token>`
- **请求参数**：
  ```json
  {
    "asset_id": 1,
    "purpose": "string"
  }
  ```
- **响应**：
  ```json
  {
    "success": true,
    "message": "借用成功",
    "data": {
      "id": 1,
      "asset": {"id": 1, "name": "string"},
      "borrow_date": "2024-01-01T00:00:00Z"
    }
  }
  ```

#### 6.6.2 归还资产
- **接口路径**：`POST /api/borrows/<record_id>/return`
- **请求头**：`Authorization: Bearer <token>`
- **响应**：
  ```json
  {
    "success": true,
    "message": "归还成功",
    "data": {
      "id": 1,
      "return_date": "2024-01-01T00:00:00Z",
      "status": "returned"
    }
  }
  ```

#### 6.6.3 获取我的借用记录
- **接口路径**：`GET /api/borrows/my`
- **请求头**：`Authorization: Bearer <token>`

### 6.7 领料管理接口

#### 6.7.1 创建领料申请
- **接口路径**：`POST /api/picks`
- **请求头**：`Authorization: Bearer <token>`
- **请求参数**：
  ```json
  {
    "item_id": 1,
    "quantity": 10,
    "purpose": "string"
  }
  ```

#### 6.7.2 审批领料申请
- **接口路径**：`POST /api/picks/<pick_id>/approve`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **请求参数**：
  ```json
  {
    "action": "approve",
    "approval_comment": "string"
  }
  ```

### 6.8 资产转移接口

#### 6.8.1 创建转移申请
- **接口路径**：`POST /api/transfers`
- **请求头**：`Authorization: Bearer <token>`
- **请求参数**：
  ```json
  {
    "asset_id": 1,
    "to_campus": "string",
    "to_building": "string",
    "to_room": "string",
    "reason": "string"
  }
  ```

#### 6.8.2 审批转移申请
- **接口路径**：`POST /api/transfers/<transfer_id>/approve`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **请求参数**：
  ```json
  {
    "action": "approve",
    "approval_comment": "string"
  }
  ```

### 6.9 统计分析接口

#### 6.9.1 获取仪表盘数据
- **接口路径**：`GET /api/dashboard`
- **请求头**：`Authorization: Bearer <token>`
- **响应**：
  ```json
  {
    "success": true,
    "data": {
      "asset_count": 100,
      "consumable_count": 50,
      "my_borrows": 5,
      "my_requests": 10,
      "low_stock_items": [
        {"id": 1, "name": "string", "stock": 5, "min_stock": 10}
      ],
      "expired_items": [
        {"id": 1, "name": "string", "expiration_date": "2024-01-01"}
      ],
      "pending_purchases": 3,
      "pending_transfers": 2,
      "pending_picks": 4
    }
  }
  ```

#### 6.9.2 获取统计数据
- **接口路径**：`GET /api/statistics`
- **请求头**：`Authorization: Bearer <token>`
- **响应**：
  ```json
  {
    "success": true,
    "data": {
      "asset_by_status": [
        {"status": "in_use", "count": 80},
        {"status": "scrapped", "count": 5},
        {"status": "repair", "count": 3},
        {"status": "returned", "count": 2},
        {"status": "borrowed", "count": 10}
      ],
      "asset_by_type": [
        {"type": "equipment", "count": 90},
        {"type": "software", "count": 10}
      ],
      "asset_by_department": [
        {"department": "计算机学院", "count": 50},
        {"department": "物理学院", "count": 30}
      ],
      "asset_by_campus": [
        {"campus": "本部", "count": 80},
        {"campus": "分部", "count": 20}
      ],
      "total_consumable_value": 50000.00,
      "low_stock_count": 5,
      "expired_count": 3,
      "purchase_by_status": [
        {"status": "pending", "count": 10},
        {"status": "approved", "count": 50},
        {"status": "rejected", "count": 5}
      ],
      "total_budget": 100000.00,
      "active_borrows": 15
    }
  }
  ```

### 6.10 基础数据接口

#### 6.10.1 获取类别列表
- **接口路径**：`GET /api/categories`
- **请求头**：`Authorization: Bearer <token>`

#### 6.10.2 获取供应商列表
- **接口路径**：`GET /api/suppliers`
- **请求头**：`Authorization: Bearer <token>`

#### 6.10.3 创建供应商
- **接口路径**：`POST /api/suppliers`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **请求参数**：
  ```json
  {
    "name": "string",
    "contact": "string",
    "phone": "string",
    "email": "string",
    "address": "string"
  }
  ```

### 6.11 操作日志接口

#### 6.11.1 获取操作日志
- **接口路径**：`GET /api/logs`
- **请求头**：`Authorization: Bearer <token>`
- **权限**：仅管理员
- **查询参数**：
  - `page` (可选)：页码
  - `page_size` (可选)：每页数量

### 6.12 通用响应格式

#### 6.12.1 成功响应
```json
{
  "success": true,
  "message": "操作成功",
  "data": {}
}
```

#### 6.12.2 错误响应
```json
{
  "success": false,
  "message": "错误信息",
  "error_code": "ERROR_CODE",
  "details": {}
}
```

#### 6.12.3 常见错误码
- `AUTH_REQUIRED`：需要登录
- `INVALID_TOKEN`：无效的token
- `PERMISSION_DENIED`：权限不足
- `VALIDATION_ERROR`：参数验证失败
- `NOT_FOUND`：资源不存在
- `INSUFFICIENT_STOCK`：库存不足
- `DUPLICATE_ENTRY`：重复数据

---

## 7. 非功能性需求

### 7.1 性能要求
- 页面响应时间 < 2秒
- 支持至少50个并发用户
- 数据库查询优化，避免N+1查询问题

### 7.2 安全要求
- 用户密码加密存储
- 防止SQL注入攻击
- 防止XSS跨站脚本攻击
- 敏感操作需要登录验证
- 管理员操作需要权限验证

### 7.3 可用性要求
- 系统可用性 > 99%
- 友好的用户界面
- 清晰的操作提示
- 完善的错误处理

### 7.4 可维护性要求
- 代码结构清晰，模块化设计
- 完善的注释文档
- 遵循Flask最佳实践
- 遵循React最佳实践
- 便于功能扩展

### 7.5 兼容性要求
- 支持主流浏览器（Chrome、Firefox、Edge、Safari）
- 响应式设计，支持移动端访问
- 支持Python 3.11+

---

## 8. 系统界面要求

### 8.1 整体风格
- 简洁、专业、现代的设计风格
- 统一的色彩方案（主色调：蓝色 #1890ff）
- 清晰的信息层级
- 良好的视觉反馈

### 8.2 导航结构
- 顶部导航栏，包含主要功能入口
- 下拉菜单用于组织相关功能
- 面包屑导航（如需要）
- 清晰的页面标题

### 8.3 响应式设计
- 支持桌面端（1920x1080及以上）
- 支持平板端（768px-1024px）
- 支持移动端（<768px）

### 8.4 交互设计
- 表单验证提示
- 操作成功/失败提示
- 加载状态指示
- 确认对话框（重要操作）

---

## 9. 部署要求

### 9.1 开发环境
- **后端**：
  - Python 3.11+
  - Flask 3.0+
  - Flask-SQLAlchemy
  - Flask-Migrate
  - Flask-CORS
  - Flask-JWT-Extended
- **数据库**：
  - SQLite 3+
- **前端**：
  - Node.js 18+
  - React 18+
  - Ant Design 5+
  - Axios
  - React Router 6+

### 9.2 生产环境
- **后端**：
  - Python 3.11+
  - Flask 3.0+
  - Gunicorn 或 uWSGI
  - SQLite 3+
- **前端**：
  - Nginx（静态文件服务）
  - 构建后的React应用
- **部署架构**：
  - Nginx + Gunicorn + Flask
  - Docker 容器化部署
  - 反向代理配置
  - SSL/HTTPS 配置

### 9.3 配置要求
- **后端配置**：
  - FLASK_ENV = production
  - SECRET_KEY（安全密钥）
  - JWT_SECRET_KEY（JWT密钥）
  - 数据库连接配置
  - CORS 配置
  - 日志记录配置
- **前端配置**：
  - API_BASE_URL（后端API地址）
  - 环境变量配置
  - 构建优化配置
- **数据库配置**：
  - 数据库文件路径配置
  - 备份策略

---

## 10. 测试要求

### 10.1 单元测试
- 模型测试
- 视图测试
- 表单测试
- 业务逻辑测试

### 10.2 集成测试
- 用户流程测试
- 权限测试
- 数据一致性测试

### 10.3 性能测试
- 页面加载时间测试
- 并发用户测试
- 数据库查询性能测试

### 10.4 安全测试
- SQL注入测试
- XSS攻击测试
- CSRF攻击测试
- 权限绕过测试

---

## 11. 未来扩展方向

### 11.1 功能扩展
- 二维码/条形码扫描功能
- 资产维护保养记录
- 资产折旧计算
- 预算管理
- 报表自定义
- 消息通知（邮件/短信）
- 移动APP

### 11.2 技术优化
- 实时数据更新（WebSocket）
- 数据可视化（ECharts/D3.js）
- 文件上传/下载
- 数据备份与恢复
- API性能优化
- 前端代码分割和懒加载
- 服务端渲染（SSR）优化

### 11.3 集成扩展
- 与财务系统集成
- 与采购系统集成
- 与库存系统集成
- 单点登录（SSO）
- 第三方认证（OAuth）

---

## 12. 附录

### 12.1 术语表
| 术语 | 说明 |
|------|------|
| 资产 | 课题组拥有的仪器设备、软件等固定资产 |
| 耗材 | 实验过程中消耗的材料、试剂等 |
| 试剂 | 实验用的化学试剂 |
| 危化品 | 危险化学品，需要特殊管理 |
| 采购申请 | 用户提交的购买新资产的申请 |
| 领料申请 | 用户申请领用耗材的申请 |
| 资产转移 | 资产从一个位置移动到另一个位置 |
| 借用记录 | 用户借用资产的历史记录 |
| 保管人变更 | 资产保管人的变更记录 |
| 资产管理人 | 负责资产管理的人员 |
| 资产现状 | 资产的当前状态（在用/报废/报修/退库/外借） |
| 审批 | 管理员对申请进行审核并做出决定 |

### 12.2 参考文档
- Flask官方文档：https://flask.palletsprojects.com/
- Flask-SQLAlchemy文档：https://flask-sqlalchemy.palletsprojects.com/
- Flask-JWT-Extended文档：https://flask-jwt-extended.readthedocs.io/
- React官方文档：https://react.dev/
- Ant Design文档：https://ant.design/
- SQLite文档：https://www.sqlite.org/docs.html
- 项目README.md

### 12.3 版本历史
| 版本 | 日期 | 说明 |
|------|------|------|
| 2.0 | 2026-03-05 | 整合资产管理系统需求更新：新增资产管理模块、资产分类（仪器设备/软件）、资产管理人功能、变动业务、维修业务、资产处置、采购/耗材字段更新 |
| 1.1 | 2026-03-02 | 技术栈更新：后端改为Flask，数据库改为SQLite，前端改为React+Ant Design，认证改为JWT |
| 1.0 | 2026-03-02 | 初始版本 |

---

**文档结束**
